#:schema https://jj-vcs.github.io/jj/latest/config-schema.json

[user]
name = "Warren Harper"
email = "warren@harperwarren.com"

[[--scope]]
--when.commands = [ "status", "diff", "log" ]
ui = {paginate = "never"}

[ui]
default-command = "log"
diff-formatter = "difft"

[mergetools.difft]
diff-args = ["--color=always", "$left", "$right"]

