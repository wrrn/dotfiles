#!/usr/bin/env fish

niri msg -j windows \
    | fx '.map(x => `${x.app_id}\t${x.title}\t${x.id}`)' list \
    | fuzzel -d --with-nth "{3}: {1} - {2}" --accept-nth 3 \
    | xargs -i -- niri msg action focus-window --id {}
